# bot module

Strategy trading bot tools. Requires API key with **Read + Trade** permissions.

> **Demo mode**: Grid endpoints are supported in demo. DCA may have limited demo support.

---

## Grid tools (5)

| Tool | Description |
|------|-------------|
| `grid_create_order` | Create a new grid bot (spot or contract) |
| `grid_stop_order` | Stop a running grid bot |
| `grid_get_orders` | List active or historical grid bots |
| `grid_get_order_details` | Get details of a specific grid bot |
| `grid_get_sub_orders` | List sub-orders (filled or live) within a grid bot |

## DCA tools (5)

Covers both Spot DCA (`spot_dca`) and Contract DCA (`contract_dca`) — distinguished by `algoOrdType`.

| Tool | Description |
|------|-------------|
| `dca_create_order` | Create a DCA (Martingale) bot — spot or contract |
| `dca_stop_order` | Stop a running DCA strategy — spot or contract |
| `dca_get_orders` | List active or historical DCA strategies — spot or contract |
| `dca_get_order_details` | Get details of a single DCA strategy — spot or contract |
| `dca_get_sub_orders` | List sub-orders / cycles generated by a DCA strategy — spot or contract |

---

## Example prompts

**Grid:**
- "List all my running grid bots"
- "Create a spot grid on BTC-USDT: 100 USDT, 10 grids, range 63000–77000"
- "Create a contract grid on BTC-USDT-SWAP: neutral, 3x leverage, 100 USDT margin, range 63000–77000"
- "Show filled sub-orders for grid bot algoId 12345"
- "Stop my BTC-USDT grid bot and sell all holdings"

**Spot DCA:**
- "Create a BTC spot DCA bot: 50 USDT initial, 30 USDT safety orders, 3 max safety, 5% price steps, 3% TP, 20% SL"
- "Show my active spot DCA strategies"
- "Stop spot DCA bot algoId 12345 and keep holdings"

**Contract DCA:**
- "Create a contract DCA on BTC-USDT-SWAP: long, 3x lever, 50 USDT init, 30 USDT safety, 3 safety orders, 2% steps, 2% TP"
- "Show my active contract DCA strategies"
- "Show cycle history for contract DCA bot algoId 12345"

---

## CLI

```bash
# ── Spot Grid (algoOrdType: grid) ─────────────────────────────────────────────
okx bot grid orders --algoOrdType grid
okx bot grid orders --algoOrdType grid --history
okx bot grid details --algoOrdType grid --algoId <id>
okx bot grid sub-orders --algoOrdType grid --algoId <id>

# Invest in quote currency (USDT)
okx bot grid create --instId BTC-USDT --algoOrdType grid \
  --maxPx 77000 --minPx 63000 --gridNum 10 --quoteSz 100

# Invest in base currency (BTC)
okx bot grid create --instId BTC-USDT --algoOrdType grid \
  --maxPx 77000 --minPx 63000 --gridNum 10 --baseSz 0.001

# Stop (stopType: 1=sell all, 2=keep holdings)
okx bot grid stop --algoId <id> --algoOrdType grid --instId BTC-USDT --stopType 2

# ── Contract Grid (algoOrdType: contract_grid) ────────────────────────────────
okx bot grid orders --algoOrdType contract_grid
okx bot grid orders --algoOrdType contract_grid --history
okx bot grid details --algoOrdType contract_grid --algoId <id>
okx bot grid sub-orders --algoOrdType contract_grid --algoId <id>

# Neutral direction, 3x leverage, 100 USDT margin
okx bot grid create --instId BTC-USDT-SWAP --algoOrdType contract_grid \
  --maxPx 77000 --minPx 63000 --gridNum 10 \
  --direction neutral --lever 3 --sz 100

# Stop (stopType: 1=close position+stop, 2=keep position+stop)
okx bot grid stop --algoId <id> --algoOrdType contract_grid --instId BTC-USDT-SWAP --stopType 1

# ── Spot DCA ──────────────────────────────────────────────────────────────────
okx bot dca orders --algoOrdType spot_dca
okx bot dca orders --algoOrdType spot_dca --history
okx bot dca details --algoOrdType spot_dca --algoId <id>
okx bot dca sub-orders --algoOrdType spot_dca --algoId <id>          # filled
okx bot dca sub-orders --algoOrdType spot_dca --algoId <id> --live   # pending

# Create spot DCA bot
okx bot dca create --algoOrdType spot_dca \
  --instId BTC-USDT \
  --initOrdAmt 50 --safetyOrdAmt 30 --maxSafetyOrds 3 \
  --pxSteps 0.05 --pxStepsMult 1 --volMult 1 \
  --tpPct 0.03 --slPct 0.20

# Stop spot DCA (stopType: 1=sell holdings, 2=keep holdings)
okx bot dca stop --algoOrdType spot_dca --algoId <id> --instId BTC-USDT --stopType 2

# ── Contract DCA ──────────────────────────────────────────────────────────────
okx bot dca orders --algoOrdType contract_dca
okx bot dca orders --algoOrdType contract_dca --history
okx bot dca details --algoOrdType contract_dca --algoId <id>
okx bot dca sub-orders --algoOrdType contract_dca --algoId <id>      # cycle list

# Create contract DCA bot
okx bot dca create --algoOrdType contract_dca \
  --instId BTC-USDT-SWAP --lever 3 --side buy \
  --initOrdAmt 50 --safetyOrdAmt 30 --maxSafetyOrds 3 \
  --pxSteps 0.02 --pxStepsMult 1 --volMult 1 --tpPct 0.02

# Stop contract DCA
okx bot dca stop --algoOrdType contract_dca --algoId <id>
```

---

## MCP server startup

```bash
okx-trade-mcp --modules bot
```

---

# bot 模块

策略交易机器人工具。需要 API Key，开启 **读取 + 交易** 权限。

> **模拟盘**：网格接口支持模拟盘。DCA 的模拟盘支持有限。

---

## 网格工具（5 个）

| 工具 | 说明 |
|------|------|
| `grid_create_order` | 创建网格机器人（现货或合约） |
| `grid_stop_order` | 停止运行中的网格机器人 |
| `grid_get_orders` | 查询运行中或历史网格机器人列表 |
| `grid_get_order_details` | 查询指定机器人详情 |
| `grid_get_sub_orders` | 查询子订单（已成交或挂单中） |

## DCA 工具（5 个）

现货 DCA（`spot_dca`）和合约 DCA（`contract_dca`）统一入口，通过 `algoOrdType` 区分。

| 工具 | 说明 |
|------|------|
| `dca_create_order` | 创建 DCA（马丁格尔）机器人 — 现货或合约 |
| `dca_stop_order` | 停止 DCA 策略 — 现货或合约 |
| `dca_get_orders` | 查询运行中或历史 DCA 策略列表 — 现货或合约 |
| `dca_get_order_details` | 查询单个 DCA 策略详情 — 现货或合约 |
| `dca_get_sub_orders` | 查询 DCA 子订单 / 补仓周期 — 现货或合约 |

---

## 示例提示词

**网格：**
- "查看我所有运行中的网格机器人"
- "在 BTC-USDT 创建现货网格：100 USDT，10 格，区间 63000–77000"
- "在 BTC-USDT-SWAP 创建合约网格：neutral 方向，3 倍杠杆，100 USDT 保证金，区间 63000–77000"
- "查看网格机器人 algoId 12345 的已成交子订单"
- "停止我的 BTC-USDT 网格机器人并卖出所有持仓"

**现货 DCA：**
- "创建 BTC 现货 DCA 机器人：初始单 50 USDT，安全单 30 USDT，最多 3 单，5% 触发跌幅，3% 止盈，20% 止损"
- "查看我的运行中现货 DCA 策略"
- "停止现货 DCA 机器人 algoId 12345 并保留持仓"

**合约 DCA：**
- "在 BTC-USDT-SWAP 创建合约 DCA：做多，3 倍杠杆，初始单 50 USDT，安全单 30 USDT，3 单，2% 触发跌幅，2% 止盈"
- "查看我的运行中合约 DCA 策略"
- "查看合约 DCA 机器人 algoId 12345 的补仓周期历史"

---

## CLI

```bash
# ── 现货网格（algoOrdType: grid）─────────────────────────────────────────────
okx bot grid orders --algoOrdType grid
okx bot grid orders --algoOrdType grid --history
okx bot grid details --algoOrdType grid --algoId <id>
okx bot grid sub-orders --algoOrdType grid --algoId <id>

# 用计价货币（USDT）投入
okx bot grid create --instId BTC-USDT --algoOrdType grid \
  --maxPx 77000 --minPx 63000 --gridNum 10 --quoteSz 100

# 用基础货币（BTC）投入
okx bot grid create --instId BTC-USDT --algoOrdType grid \
  --maxPx 77000 --minPx 63000 --gridNum 10 --baseSz 0.001

# 停止（stopType: 1=卖出全部持仓, 2=保留持仓）
okx bot grid stop --algoId <id> --algoOrdType grid --instId BTC-USDT --stopType 2

# ── 合约网格（algoOrdType: contract_grid）────────────────────────────────────
okx bot grid orders --algoOrdType contract_grid
okx bot grid orders --algoOrdType contract_grid --history
okx bot grid details --algoOrdType contract_grid --algoId <id>
okx bot grid sub-orders --algoOrdType contract_grid --algoId <id>

# neutral 方向，3倍杠杆，100 USDT 保证金
okx bot grid create --instId BTC-USDT-SWAP --algoOrdType contract_grid \
  --maxPx 77000 --minPx 63000 --gridNum 10 \
  --direction neutral --lever 3 --sz 100

# 停止（stopType: 1=平仓并停止, 2=保留仓位并停止）
okx bot grid stop --algoId <id> --algoOrdType contract_grid --instId BTC-USDT-SWAP --stopType 1

# ── 现货 DCA ──────────────────────────────────────────────────────────────────
okx bot dca orders --algoOrdType spot_dca
okx bot dca orders --algoOrdType spot_dca --history
okx bot dca details --algoOrdType spot_dca --algoId <id>
okx bot dca sub-orders --algoOrdType spot_dca --algoId <id>          # 已成交子订单
okx bot dca sub-orders --algoOrdType spot_dca --algoId <id> --live   # 挂单中子订单

# 创建现货 DCA 机器人
okx bot dca create --algoOrdType spot_dca \
  --instId BTC-USDT \
  --initOrdAmt 50 --safetyOrdAmt 30 --maxSafetyOrds 3 \
  --pxSteps 0.05 --pxStepsMult 1 --volMult 1 \
  --tpPct 0.03 --slPct 0.20

# 停止现货 DCA（stopType: 1=卖出持仓, 2=保留持仓）
okx bot dca stop --algoOrdType spot_dca --algoId <id> --instId BTC-USDT --stopType 2

# ── 合约 DCA ──────────────────────────────────────────────────────────────────
okx bot dca orders --algoOrdType contract_dca
okx bot dca orders --algoOrdType contract_dca --history
okx bot dca details --algoOrdType contract_dca --algoId <id>
okx bot dca sub-orders --algoOrdType contract_dca --algoId <id>      # 补仓周期列表

# 创建合约 DCA 机器人
okx bot dca create --algoOrdType contract_dca \
  --instId BTC-USDT-SWAP --lever 3 --side buy \
  --initOrdAmt 50 --safetyOrdAmt 30 --maxSafetyOrds 3 \
  --pxSteps 0.02 --pxStepsMult 1 --volMult 1 --tpPct 0.02

# 停止合约 DCA
okx bot dca stop --algoOrdType contract_dca --algoId <id>
```

---

## MCP 服务启动

```bash
okx-trade-mcp --modules bot
```
